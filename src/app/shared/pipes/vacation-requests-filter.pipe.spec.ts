import { VacationRequestsFilterPipe } from './vacation-requests-filter.pipe';

describe('VacationRequestsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new VacationRequestsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
