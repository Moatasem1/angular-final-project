import { HighlightPipe } from './highlight.pipe';

describe('VacayHighlightPipe', () => {
  it('create an instance', () => {
    const pipe = new HighlightPipe();
    expect(pipe).toBeTruthy();
  });
});
